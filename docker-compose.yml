version: '3'

services:
  webserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webserver
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - app-network
  
networks:
  app-network:
    driver: bridge
